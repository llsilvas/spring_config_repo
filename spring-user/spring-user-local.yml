spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userdb
    username: root 
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

app: 
  message: Local message Teste